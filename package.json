{
  "name": "csc-307-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npm -w react-frontend run dev",
    "backend": "npm -w express-backend run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "dependencies": {
    "mongoose": "^8.8.0"
  }
}
